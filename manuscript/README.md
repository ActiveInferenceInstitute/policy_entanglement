# `manuscript/` — Modular markdown sections

Each markdown file here corresponds to a numbered section of the
manuscript "Policy Entanglement in Active Inference".  The
template's PDF rendering pipeline concatenates them in lexical order to
produce the final PDF.

The manuscript is organized into **six IMRAD parts**.  Body files use a
`<digit><letter>_*.md` prefix that encodes their part membership; Part
divider files (`1A_*`, `2A_*`, …, `6A_*`) carry an unnumbered Part
heading and a one-paragraph orientation.

**Current state.**  28 front/body/part section files (front matter
`0A`; Part I `1A`–`1C`; Part II `2A`–`2J`; Part III `3A`–`3B`; Part IV
`4A`–`4E`; Part V `5A`–`5D`; Part VI `6A`–`6C`), 8 supplements
(`S01`–`S08`), 1 auto-generated bibliography (`99_bibliography.md`)
sourced from [`refs/citations.yaml`](refs/citations.yaml).  Citation
and rendered-bibliography counts are generated by
[`../scripts/manuscript_variables.py`](../scripts/manuscript_variables.py)
and recorded in `output/data/manuscript_variables.json`; do not
hand-maintain them here.  Every numbered theorem in this manuscript
ships a live Lean companion in the `ActinfPolicyEntanglement` boundary
fragment, with status counts generated from
[`refs/labels.yaml`](refs/labels.yaml) into the same manuscript
variables sidecar.

**Contract — no hardcoded numbers**: every numeric value in prose, plus
the few controlled categorical descriptors used for stress-test
variants, is auto-injected from a real analytical / pymdp run via the
`[[VAR:<key>]]` token system, sourced from
[`../src/simulation/hyperparameters.py`](../src/simulation/hyperparameters.py)
and mirrored into `output/data/manuscript_variables.json`.  Sections,
figures, equations, and theorem references all flow through the
registry (`refs/labels.yaml`) — there are no hand-written `§4.1`,
`Fig. 7`, `Eq. (3)`, or `Theorem 5.1` strings in prose, headings,
captions, short labels, or registry titles.  CI-gated by
[`../scripts/validate_manuscript.py`](../scripts/validate_manuscript.py)
(`find_hardcoded_numeric_literals`, `find_hardcoded_refs`, and
`find_hardcoded_rendered_source_literals`).  Rendered output may
contain the resolved display labels and numeric values; source files
must carry the tokens or neutral wording.  See
[`../docs/guides/styleguide.md`](../docs/guides/styleguide.md) for
the full contract and
[`../docs/reference/veridical_status.md`](../docs/reference/veridical_status.md)
for the live audit chain.

## File map

The auto-generated table of contents lives at
[`INDEX.md`](INDEX.md) and is regenerated by
[`../scripts/generate_index.py`](../scripts/generate_index.py).  The
summary below shows the IMRAD layout.

| Part | Files | Subject |
|---|---|---|
| **Front matter** | [`preamble.md`](preamble.md), [`0A_abstract.md`](0A_abstract.md) | LaTeX preamble + author front-matter; one-page abstract |
| **Part I — Introduction** | [`1A_part1_introduction.md`](1A_part1_introduction.md), [`1B_motivation.md`](1B_motivation.md), [`1C_background_and_prior_work.md`](1C_background_and_prior_work.md) | §1 Motivation; §2 Background and prior work |
| **Part II — Theory** | [`2A_part2_theory.md`](2A_part2_theory.md), [`2B_setup.md`](2B_setup.md) … [`2J_comparative_statics.md`](2J_comparative_statics.md) | §3 Setup → §11 Comparative statics (definitions, theorems, examples) |
| **Part III — Formal verification** | [`3A_part3_formal_verification.md`](3A_part3_formal_verification.md), [`3B_lean_formalization.md`](3B_lean_formalization.md) | §12 Lean 4 formalization plan |
| **Part IV — Empirical grounding** | [`4A_part4_empirical_grounding.md`](4A_part4_empirical_grounding.md), [`4B_empirical_suite.md`](4B_empirical_suite.md) … [`4E_pymdp_validation.md`](4E_pymdp_validation.md) | §13 Empirical suite, §14 pymdp harness, §15 free-energy bundle, §16 validation |
| **Part V — Connections to existing frameworks** | [`5A_part5_connections.md`](5A_part5_connections.md), [`5B_connections_aif.md`](5B_connections_aif.md), [`5C_connections_control_rl.md`](5C_connections_control_rl.md), [`5D_connections_multi_agent.md`](5D_connections_multi_agent.md) | §17–§19: classical AIF, control + RL, multi-agent + geometry |
| **Part VI — Discussion and outlook** | [`6A_part6_discussion.md`](6A_part6_discussion.md), [`6B_open_questions.md`](6B_open_questions.md), [`6C_discussion_and_outlook.md`](6C_discussion_and_outlook.md) | §20 Open questions; §21 Discussion and outlook |
| **References** | [`99_bibliography.md`](99_bibliography.md) | Auto-generated from `refs/citations.yaml` |
| **Supplements** | [`S01`](S01_proof_of_decomposition_theorem.md) … [`S08`](S08_gnn_generalized_notation_extension.md) | Appendices A–H: proofs, derivations, Lean source skeleton, notation, reference tables, GNN shipped fifth track (parser + verified round-trip + Lean typed-contract emitter) |

The full per-file table is in [`INDEX.md`](INDEX.md).

## Authoring conventions

* **Cross-reference Lean modules.**  When a section formally states or
  motivates a Lean theorem, link to the file under
  [`../lean/ActinfPolicyEntanglement/`](../lean/ActinfPolicyEntanglement/).
* **Cross-reference figures.**  Manuscript figures are produced by
  [`../scripts/generate_figures.py`](../scripts/generate_figures.py)
  and [`../scripts/simulate_pymdp.py`](../scripts/simulate_pymdp.py)
  and end up at `../output/figures/*.png`.  Embed them with the
  registry token `[[FIG:label]]` (where `label` is a key under
  `figures:` in [`refs/labels.yaml`](refs/labels.yaml)) — the
  injection pipeline substitutes the full Markdown image directive
  with the registered caption.  Inline cross-references use
  `[[FIGREF:label]]`.
* **Manuscript variables.**  In-text variable substitutions use
  the `[[VAR:key]]` or `[[VAR:key:fmt]]` token, where `key` is a
  field in [`../output/data/manuscript_variables.json`](../output/data/manuscript_variables.json)
  emitted by [`../scripts/manuscript_variables.py`](../scripts/manuscript_variables.py).
* **Section / theorem references.**  Use `[[SECREF:label]]` for inline
  section references and `[[THMREF:label]]` for theorems — both resolve
  through the registry, so renumbering a section in `refs/labels.yaml`
  propagates everywhere automatically.
* **Citations.**  Use Pandoc-style `[@citekey]` referring to entries
  in [`refs/citations.yaml`](refs/citations.yaml); the bibliography
  body of [`99_bibliography.md`](99_bibliography.md) is auto-generated
  from the same source via `[[CITELIST:all]]`.
* **Determinism.**  Don't paste random numbers from notebooks; cite a
  scripted figure or a manuscript variable.

## Source-of-truth note

The modular markdown sections in this directory are the **canonical**
manuscript.  They are concatenated in lexical order by the template
PDF rendering pipeline.  Filename prefixes (`<digit><letter>_*`) encode
IMRAD-part membership and are also the lexical sort key the renderer
uses; the registry under [`refs/labels.yaml`](refs/labels.yaml) is the
single source of truth for displayed section numbers, theorem
numbers, figure metadata, and citation keys.

## Building the PDF

The template-pipeline rendering job concatenates these files via
`infrastructure/rendering/pdf_renderer.py`.  Locally:

```bash
cd ../..   # back to template root
./run.sh --project actinf_policy_entanglement_lean --pipeline
```
