# AGENTS.md — `docs/_audit/`

This directory is an evidence archive, not the live documentation source
of truth.

- Preserve dated audit files as written unless the user explicitly asks
  for historical correction.
- Add supersession notes or newer audit artifacts instead of rewriting
  old evidence.
- Keep current-facing status, methods, and signposting in the root docs,
  `docs/reference/`, the manuscript, and generated reports.
- Do not relax global documentation guards to hide drift discovered here;
  either scope the historical file explicitly or update the current-facing
  surface that drifted.

