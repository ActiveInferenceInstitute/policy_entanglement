"""Pipeline orchestration (library layer).

Subpackage for end-to-end pipeline coordination logic that the thin scripts
under ``../../scripts/`` re-export. Currently holds the canonical
end-to-end runner (see :mod:`orchestration.run_all`).
"""
