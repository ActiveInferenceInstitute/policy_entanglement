# `src/visualizations/`

Plotting helpers consumed by scripts (heatmaps, joints, spectra, trajectories). I/O stays in [`scripts/`](../../scripts/); modules here expose figure builders — see [`AGENTS.md`](AGENTS.md).
